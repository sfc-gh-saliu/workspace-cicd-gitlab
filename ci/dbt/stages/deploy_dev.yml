# TODO: implement dbt DEV deployment (dbt run against DEV target)
dbt_deploy_to_dev:
  stage: deploy_dev
  needs:
    - dbt_validate
  script:
    - 'echo "=== [stage: deploy_dev] [job: dbt_deploy_to_dev] STARTED ==="'
    - echo "dbt deploy to DEV — not yet implemented"
    - 'echo "=== [stage: deploy_dev] [job: dbt_deploy_to_dev] FINISHED ==="'
