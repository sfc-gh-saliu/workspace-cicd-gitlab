# TODO: implement dbt PROD deployment (dbt run against PROD target)
dbt_deploy_to_prod:
  stage: deploy_prod
  needs:
    - dbt_deploy_to_dev
  script:
    - 'echo "=== [stage: deploy_prod] [job: dbt_deploy_to_prod] STARTED ==="'
    - echo "dbt deploy to PROD — not yet implemented"
    - 'echo "=== [stage: deploy_prod] [job: dbt_deploy_to_prod] FINISHED ==="'
