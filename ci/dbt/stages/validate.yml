# TODO: implement dbt validation (compile, lint, test)
dbt_validate:
  stage: validate
  script:
    - 'echo "=== [stage: validate] [job: dbt_validate] STARTED ==="'
    - echo "dbt validate — not yet implemented"
    - 'echo "=== [stage: validate] [job: dbt_validate] FINISHED ==="'
